'use strict';
/*

 describe('kinetics-problems.firstOrderPostdict firstOrderPostdictCtrl', function () {
 beforeEach(module('kinetics-problems'));
 beforeEach(module('kinetics-problems.firstOrderPostdict'));

 var ctrl, scope;
 // inject the $controller and $rootScope services
 // in the beforeEach block
 beforeEach(inject(function ($controller, $rootScope) {
 // Create a new scope that's a child of the $rootScope
 scope = $rootScope.$new();
 // Create the controller
 ctrl = $controller('firstOrderPostdictCtrl', {
 $scope: scope
 });
 }));
 it('should exist....',
 function () {
 //spec body
 expect(ctrl).toBeDefined();
 });
 });


 */
